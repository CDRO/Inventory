{
  "name": "inventory-e2e",
  "private": true,
  "description": "Playwright specs for the E2E deployment gate (docs/specs/05-frontend-pwa-foundations.md). Runs only inside the throwaway Playwright container defined in docker-compose.e2e.yml — this file is never installed on the host and is not the frontend's build step; the frontend has none (docs/specs/05-frontend-pwa-foundations.md).",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "1.48.0"
  },
  "dependencies": {
    "playwright-core": "^1.48.0"
  }
}
